var documenterSearchIndex = {"docs":
[{"category":"section","location":"semantics/#Semantics","page":"Semantics","text":"This page is the contract: what each operation returns, and what happens at the edges. Nothing here loses a digit without saying so. Where a result cannot be exact, either a rounding mode was passed or an exception is thrown.","title":"Semantics"},{"category":"section","location":"semantics/#The-rules-in-one-table","page":"Semantics","text":"operation rule\n+, - promote both operands value-preservingly, then compute checked: OverflowError, never a silent round or wrap\n* exact; result scale is S1 + S2, precision min(P1+P2, 76), checked\n* by an Integer scale-preserving; the precision grows by the integer's digit count\n/ rounds half-even at scale max(S1, S2); DataDecimals.divide(x, y, mode) for any other mode\nsum accumulates in a wider storage tier, where overflow is provably impossible (below 1.7e20 elements for a 64-bit-tier column), so the reduction is both safe and vectorizable\ndiv, rem, mod, fld, cld, divrem real implementations in every rounding mode, not float fallbacks\nround, trunc, floor, ceil exact and in-type, with a digits keyword; no sigdigits\nconvert, numeric constructors integer/rational/decimal sources are exact or throw; float sources round half-even\nDecimal{P,S}(::AbstractFloat) rounds half-even at scale S, as cross-representation float conversions in Base do\nDataDecimals.DecimalValue(::AbstractFloat) the exact binary expansion, which is what a runtime scale is for\nparse, tryparse provided by the Parsers extension; round half-even at the target scale, like parse(Float64, s); tryparse returns nothing\nDataDecimals.rescale, round(D, x, mode) the explicit rounding conversions between decimal types\n==, <, hash by numeric value across scales, precisions, and number types\n≈ exact equality between decimals (rtoldefault is 0 for a non-AbstractFloat Real, as for Rational)\nstring, print, writedecimal! always the plain positional form\nshow (text/plain) positional, switching to scientific notation past 44 characters\nrounding state there is none","title":"The rules in one table"},{"category":"section","location":"semantics/#Why-checked,-not-rounding","page":"Semantics","text":"The 0.x package, like Python's decimal, rounded every operation to a dynamically scoped precision. That is a correctness trap in the applications that reach for a decimal type: a sum quietly loses its low digits and nothing says so. Rational is Base's own precedent that throwing is acceptable behaviour for an exact type, and FixedPointDecimals shows what the other failure mode, wrapping on overflow, costs.\n\nSo +, -, and * throw OverflowError rather than round or wrap. The cost is a branch per scalar operation, which is why sum and the broadcast kernels are specialised: they widen or block-check so that the common columnar reductions stay branch-free and vectorized while remaining safe. A hand-written reduce(+, v) over a narrow type pays the checked-operation cost; that is the price of never wrapping.","title":"Why checked, not rounding"},{"category":"section","location":"semantics/#No-global-state","page":"Semantics","text":"Rounding is never a mode to switch on. There is no context object, no scoped value, no setrounding. Wherever a rounding decision has to be made, the mode is an argument:\n\nDataDecimals.divide(x, y, mode) instead of /\nDataDecimals.rescale(D, x, mode), DataDecimals.rescale(v, s, mode)\nround(D, x, mode), round(I, x, mode) for integer targets\nround(x, mode; digits) within a type\nrounding= on Parsers.parse\n\nThe seven accepted modes are RoundNearest (half-even, the default), RoundNearestTiesAway, RoundNearestTiesUp, RoundToZero, RoundFromZero, RoundDown, and RoundUp. An eighth, DataDecimals.RoundExact, requires exactness: it throws InexactError instead of dropping a digit, Parsers.tryparse returns nothing, and Parsers.parsenext returns Parsers.RC_INVALID.\n\nA dynamic context costs dispatch on every operation, and it makes a function's result depend on its caller. Neither seemed worth paying for.","title":"No global state"},{"category":"section","location":"semantics/#Result-types","page":"Semantics","text":"Arithmetic never invents a type wider than the promotion of its inputs, so folds are type-stable. The result-type rules:\n\nexpression result\nDecimal{P1,S1} + Decimal{P2,S2} promote_type of the two: scale max(S1,S2), integer digits max(P1-S1, P2-S2), capped at 76\nDecimal{P1,S1} * Decimal{P2,S2} scale S1+S2, precision min(P1+P2, 76)\nDecimal{P,S} * ::Integer scale S, precision min(P + digits(I), 76)\nDecimal{P1,S1} / Decimal{P2,S2} scale max(S1,S2), precision sized for the widest possible quotient\nsum(::Vector{Decimal{P,S,T}}) Decimal{38,S,Int128} for the 32- and 64-bit tiers, Decimal{76,S,Int256} for the 128-bit tier, unchanged for Int256\ndecimal ⊕ AbstractFloat the float type; floats win promotion\ndecimal ⊕ Rational{I} Rational, the exact type that can hold the result\nDataDecimals.DecimalValue{T1} ⊕ DataDecimals.DecimalValue{T2} DataDecimals.DecimalValue{promote_type(T1,T2)}, scale max\nDataDecimals.DecimalValue ⊕ Decimal DataDecimals.DecimalValue with the promoted storage type\n\nThe 76-digit cap is where the Int256 tier ends; an operation whose exact result would need more digits throws rather than truncating.","title":"Result types"},{"category":"section","location":"semantics/#Conversions","page":"Semantics","text":"Out of a decimal:\n\ntarget behaviour\nFloat64, Float32, Float16 correctly rounded, no double-rounding, no MPFR on the fast path\nBigFloat exact at any requested precision and MPFR rounding mode\nRational{I} exact, in lowest terms; Rational(::Decimal) uses the storage type as I\nbig(x) Rational{BigInt}; a BigFloat would re-round the fraction in binary\nInt, BigInt, any Integer exact or InexactError; round/trunc/floor/ceil with an Integer type argument are the rounding forms\n\nInteger(x) returns the value in the storage tier's own integer type, so Integer(DataDecimals.Decimal64{2}(\"5.00\")) is an Int64 and Integer(DataDecimals.Decimal256{2}(\"5.00\")) is a DataDecimals.Int256. Ask for Int(x) when you want an Int.\n\nInteger, rational, and decimal sources convert exactly or throw. Float construction and conversion, string construction, and parse round half-even at the target scale. Runtime-scale float construction is exact or throws.","title":"Conversions"},{"category":"section","location":"semantics/#Equality,-ordering,-and-hashing","page":"Semantics","text":"Comparison is exact and cross-type. dec\"1.20\", dec\"1.2\", 12//10, and DataDecimals.Decimal128{9}(\"1.200000000\") all compare equal, and all hash equal. Comparisons against floats are done exactly, by cross-multiplying in the 2/5-power domain rather than promoting the decimal to Float64, so a decimal too wide for a Float64 still compares correctly. dec\"1.2\" == 1.2 is therefore false, for the same reason 12//10 == 1.2 is false in Base: the Float64 nearest to 1.2 is not 1.2.\n\nHashing goes through Base.decompose, the same hook Rational and the float types use, which is what makes Dict, Set, unique, searchsorted, and DataFrames groupby/join behave across mixed numeric types.\n\nOrdering is total. isless and < agree; there is no NaN to make them differ. Sorting a decimal vector is a comparison on the unscaled integer when the scales match.","title":"Equality, ordering, and hashing"},{"category":"section","location":"semantics/#Text","page":"Semantics","text":"string, print, and writedecimal! always emit the plain positional form, so the output is at once a valid SQL literal, CSV field, and JSON number, and always round-trips through parse.\n\nDisplay is separate: show(io, MIME\"text/plain\", x), which is what the REPL calls, prints the positional form up to 44 characters and switches to scientific notation beyond it, so a scale-1000 DataDecimals.DecimalValue does not fill a screen with zeros. Two-argument show gives the typed constructor form, Decimal{18,2,Int64}(\"1234.56\"), which is what repr and container display use.\n\nPrintf's %f/%e/%g round in decimal before conversion to a sufficiently precise BigFloat; %d is exact-or-InexactError. JSON.json emits exact digits. Both are package extensions.","title":"Text"},{"category":"section","location":"semantics/#The-implicit-Real-interface","page":"Semantics","text":"A study of what Base and the ecosystem actually call on a custom Real produced a 25-item checklist. The load-bearing fallback chains are Base.decompose (feeding hash, isfinite, and Rational comparison), Base.AbstractFloat/float (feeding norms, plotting, and histograms), the T(::T) identity constructor (feeding range getindex, oneunit, and DiffEq), and < rather than isless (Base derives isless from < for Reals).\n\nEvery item is implemented, with two departures from the usual defaults, exact ≈ and the absence of NaN/Inf, and one open item: a Plots/Makie type recipe that would give exact-digit tick labels. Plotting works today through the Float64 path.","title":"The implicit Real interface"},{"category":"section","location":"semantics/#Ecosystem-behaviour","page":"Semantics","text":"docs/ecosystem-compat.md in the repository has the full report: 117 trials against real packages, plus a comparison with PostgreSQL, DuckDB, SQL Server, Spark, DataFusion, Arrow C++, pandas, and Python decimal for every operation where more than one return type is defensible.\n\n115 of the 117 trials pass. Base numerics, collections, ranges, DataFrames (groupby, four join types, combine, sorting with missing), Statistics, LinearAlgebra, Random, ForwardDiff, JuMP, Unitful, StaticArrays, and JSON all work, as do the edge-case probes (-0.0 isequal, empty-range dead-branch arithmetic, promotion round-trips, and comparisons against floats too wide for Float64).","title":"Ecosystem behaviour"},{"category":"section","location":"semantics/#What-analytics-operations-return","page":"Semantics","text":"operation result precedent\nsum exact decimal, accumulated in a wider storage tier universal; every engine widens sum at fixed scale\nmean same-scale decimal, half-even rounded the Arrow C++ camp; PostgreSQL and Spark raise the scale, DuckDB goes to DOUBLE\nmedian, middle decimal (rounds only if the midpoint needs an extra digit) DuckDB: median(DECIMAL(10,2)) → DECIMAL(10,2)\nvar, cov, kurtosis decimal, widened as the intermediate squares require the same as Rational in Base\nstd, cor, skewness Float64, since each ends in a square root or a ratio of roots the same as Rational in Base\nquantile(v, p::Float64) Float64; an exact p::Rational stays exact the same as Rational in Base\nnorm, hypot, sqrt, exp, log, trig Float64 settled Base convention (norm([3//1,4//1]) == 5.0)\noverflow anywhere OverflowError Rational is Base's precedent for throwing","title":"What analytics operations return"},{"category":"section","location":"semantics/#LinearAlgebra","page":"Semantics","text":"A*B, dot, tr, A+B, and kron stay exact decimal. Factorizations convert to Float64 first, for three reasons:\n\nA generic lu! on a fixed-scale decimal either throws InexactError mid-elimination or silently corrupts the answer: the measured determinant of a matrix whose true determinant is -2 came back as -2.0004, because every elimination step rounds at scale and the pivoting decisions compound it.\nNo system in the comparison runs LAPACK-style algorithms in fixed decimal. Eigensolvers are float-only even for DecFP, whose maintainers point users at Float64; eigenvalues carry roundoff regardless, so decimal exactness buys nothing there.\nRational's exact lu works only because Rational grows unboundedly. A fixed-scale type cannot offer exact elimination.\n\nFor a generic-precision factorization, ask for one explicitly: lu(Rational.(A)), or GenericLinearAlgebra.jl over big.(A).","title":"LinearAlgebra"},{"category":"section","location":"semantics/#Unitful","page":"Semantics","text":"Quantity{Decimal} arithmetic and integer-factor conversions (m to cm) stay decimal. A conversion factor that is a Rational{Int} promotes the value to Rational: exact, never throwing, never silently rounding. There is no *(Decimal, Rational) -> Decimal closure rule, because that would force rounding inside a library users chose for correctness. Convert back at the end if the result must be decimal.","title":"Unitful"},{"category":"section","location":"semantics/#The-two-known-failures","page":"Semantics","text":"Both are upstream requirements, and both fail identically for Rational:\n\nStatsBase.summarystats and describe require AbstractFloat input. summarystats(float.(v)) is the answer.\nAdaptive ODE solvers (Tsit5() and its neighbours). SciML documents that adaptive time types must be floating point, and adaptivity uses NaN as a reject-step sentinel, which no exact type has. Non-adaptive solvers with exactly-representable coefficients, such as Euler() with dt = dec\"0.1\", work and are exact. For anything adaptive, solve(remake(prob, u0=float.(u0)), …).\n\nMore broadly, the domains where conversion to float happens automatically are eigvals/eigen/svd/Schur, adaptive ODE, JuMP solver internals, summarystats, and plot coordinates. These are domains where comparable systems convert as well, documented here rather than left to be discovered.","title":"The two known failures"},{"category":"section","location":"semantics/#Performance-notes","page":"Semantics","text":"The kernel-level results carry through generic code: sum, mean, and broadcast over million-element columns stay allocation-free with the SIMD paths engaged; DataFrames groupby/join hit the decompose-based hash without falling back to BigInt; sorting is a comparison on the unscaled integer.\n\nThe one thing to keep in mind is the checked-+ cost in a hand-written reduction over a narrow type. sum sidesteps it by widening to a tier where overflow is provably impossible, but a custom reduce(+, v) cannot. That is the price of never wrapping. bench/PERF.md has the measurements.","title":"Performance notes"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Types","page":"API Reference","text":"","title":"Types"},{"category":"section","location":"api/#Literals","page":"API Reference","text":"","title":"Literals"},{"category":"section","location":"api/#Rounding-and-rescaling","page":"API Reference","text":"","title":"Rounding and rescaling"},{"category":"section","location":"api/#Wire-and-byte-level-API","page":"API Reference","text":"","title":"Wire and byte-level API"},{"category":"section","location":"api/#Base-methods","page":"API Reference","text":"These Base functions have methods for the decimal types; they are documented by Base and behave as described in Semantics.\n\nConstruction and conversion : convert, reinterpret, parse and tryparse (via the Parsers extension), promote_rule,   Float64 / Float32 / Float16 / BigFloat / AbstractFloat, Rational,   Integer and the concrete integer types, big, rationalize, widen.\n\nArithmetic : +, -, *, /, div, rem, mod, fld, cld, divrem, sum,   abs, Base.Checked.checked_add / checked_sub / checked_mul /   checked_neg / checked_abs.\n\nRounding : round, trunc, floor, ceil, in both the in-type forms (with a   digits keyword) and the converting forms round(T, x, mode).\n\nComparison and hashing : ==, <, <=, isless, Base.decompose, hash, signbit, iszero,   isinteger, isfinite, isnan, isinf.\n\nType traits and constants : zero, one, eps, typemin, typemax, floatmin, floatmax,   precision.\n\nText : string, print, show.\n\nOther : Random.rand (uniform on [0, 1) at the type's scale), and broadcast   kernels for elementwise +, -, * over vectors.","title":"Base methods"},{"category":"section","location":"api/#Extension-entry-points","page":"API Reference","text":"Available when the corresponding package is loaded; see Extensions in the manual.\n\nParsers.parse, Parsers.tryparse, Parsers.parsenext : byte-level parsing with decimal= and rounding= keywords (Parsers 3).\n\nPrintf.tofloat, Printf.toint : exact %f / %e / %g and exact-or-InexactError %d.\n\nJSON.tostring : JSON numbers carrying the exact digits (JSON 1.x).\n\nLinearAlgebra factorizations : lu, cholesky, qr, svd, eigen, hessenberg, schur, lq, det,   logdet, inv, eigvals, svdvals, cond, nullspace, pinv, rank   route through float(A).","title":"Extension entry points"},{"category":"section","location":"api/#Index","page":"API Reference","text":"","title":"Index"},{"category":"type","location":"api/#DataDecimals.AbstractDecimal","page":"API Reference","text":"DataDecimals.AbstractDecimal <: Real\n\nSupertype of the package's exact decimal types, Decimal (scale fixed in the type) and DataDecimals.DecimalValue (scale carried per value). Every AbstractDecimal is an exact rational of the form unscaled(x) * 10^-scale(x); there are no NaN or Inf values, so isfinite is always true and isnan always false.\n\n\n\n\n\n","title":"DataDecimals.AbstractDecimal"},{"category":"type","location":"api/#DataDecimals.Decimal","page":"API Reference","text":"Decimal{P, S, T} <: DataDecimals.AbstractDecimal\nDecimal{P, S}\n\nAn exact fixed-scale decimal: the value is unscaled * 10^-S, with the invariant |unscaled| < 10^P. P is the precision (total decimal digits), S the scale (fractional digits, 0 <= S <= P), and T the storage integer — Int32, Int64, Int128, or DataDecimals.Int256 for P up to 9, 18, 38, or 76 respectively. Writing Decimal{P,S} fills T in from P.\n\nThe struct holds nothing but the storage integer, so Vector{Decimal{P,S,T}} can share an Arrow coefficient buffer of the same width and host byte order. Other wire formats require their own encoding. See DataDecimals.Decimal64 and its siblings for the aliases naming those tiers.\n\nConstructors are exact or they throw: Decimal{18,2}(5), Decimal{18,2}(1//4), and convert raise InexactError/OverflowError rather than round. The exceptions are construction from AbstractFloat, which rounds half-even at scale S like every cross-representation float conversion in Base, and construction from a string, which is parse (also half-even). DataDecimals.rescale and round(D, x, mode) are the explicitly-rounding forms.\n\nreinterpret(Decimal{P,S,T}, u) builds a value straight from an unscaled coefficient without checking the |u| < 10^P invariant — the zero-copy wire decode path, where the invariant is the caller's contract.\n\njulia> Decimal{18,2}(\"1234.56\")\n1234.56\n\njulia> Decimal{18,2}(5) === reinterpret(Decimal{18,2,Int64}, 500)\ntrue\n\nValues display as plain digits; repr/show give the round-trippable typed form Decimal{18,2,Int64}(\"1234.56\").\n\n\n\n\n\n","title":"DataDecimals.Decimal"},{"category":"type","location":"api/#DataDecimals.DecimalValue","page":"API Reference","text":"DataDecimals.DecimalValue{T} <: DataDecimals.AbstractDecimal\nDataDecimals.DecimalValue(unscaled, scale)\n\nAn exact decimal whose scale travels with the value instead of with the type: unscaled * 10^-scale, where scale is an Int32 in 0:16383. T is the storage integer (Int32, Int64, Int128, or DataDecimals.Int256); the bare DataDecimals.DecimalValue(u, s) constructor infers it from u, defaulting to Int64.\n\nThis is the type for row-oriented wire values that carry their own scale — a PostgreSQL numeric's per-value dscale, or the output of normalize — and for values whose scale is not known until runtime. It is still isbits, but it is one word wider than Decimal and its arithmetic aligns scales at run time rather than at compile time, so prefer Decimal{P,S} for columnar data.\n\nLike Decimal, conversions in are exact-or-throw except from AbstractFloat, where DataDecimals.DecimalValue(x) produces the exact binary expansion of x (that is what a runtime scale is for) rather than rounding to a fixed scale. DataDecimals.rescale moves a value to another scale.\n\njulia> DataDecimals.DecimalValue(12345, 3)\n12.345\n\njulia> repr(DataDecimals.DecimalValue(0.1))\n\"DataDecimals.DecimalValue{DataDecimals.Int256}(\\\"0.1000000000000000055511151231257827021181583404541015625\\\")\"\n\n\n\n\n\n","title":"DataDecimals.DecimalValue"},{"category":"type","location":"api/#DataDecimals.Decimal32","page":"API Reference","text":"DataDecimals.Decimal32{S}\n\nAlias for Decimal{9,S,Int32} — the widest precision the 32-bit storage tier holds. See DataDecimals.Decimal64 for the family.\n\n\n\n\n\n","title":"DataDecimals.Decimal32"},{"category":"type","location":"api/#DataDecimals.Decimal64","page":"API Reference","text":"DataDecimals.Decimal64{S}\nDataDecimals.Decimal32{S}\nDataDecimals.Decimal128{S}\nDataDecimals.Decimal256{S}\n\nAliases for the four storage tiers at their maximum precision:\n\nalias expands to digits bits\nDataDecimals.Decimal32{S} Decimal{9,S,Int32} 9 32\nDataDecimals.Decimal64{S} Decimal{18,S,Int64} 18 64\nDataDecimals.Decimal128{S} Decimal{38,S,Int128} 38 128\nDataDecimals.Decimal256{S} Decimal{76,S,DataDecimals.Int256} 76 256\n\nS is the scale, so DataDecimals.Decimal64{2} is the usual money type. Use Decimal{P,S} directly when a schema pins a precision narrower than the tier's maximum.\n\njulia> DataDecimals.Decimal64{2} === Decimal{18,2,Int64}\ntrue\n\n\n\n\n\n","title":"DataDecimals.Decimal64"},{"category":"type","location":"api/#DataDecimals.Decimal128","page":"API Reference","text":"DataDecimals.Decimal128{S}\n\nAlias for Decimal{38,S,Int128}. See DataDecimals.Decimal64 for the family.\n\n\n\n\n\n","title":"DataDecimals.Decimal128"},{"category":"type","location":"api/#DataDecimals.Decimal256","page":"API Reference","text":"DataDecimals.Decimal256{S}\n\nAlias for Decimal{76,S,DataDecimals.Int256}, the widest tier. See DataDecimals.Decimal64 for the family.\n\n\n\n\n\n","title":"DataDecimals.Decimal256"},{"category":"macro","location":"api/#DataDecimals.@dec_str","page":"API Reference","text":"@dec_str -> Decimal\ndec\"1.25\"\n\nAn exact decimal literal. Produces the minimal-fitting Decimal{P,S} (so dec\"1.25\" isa Decimal{3,2,Int32}), constant-folded at parse time; literals beyond 76 digits of precision fall back to a DataDecimals.DecimalValue{Int256}. Digit groups may be separated with underscores (dec\"1_000_000.25\"). The literal must be exactly representable — no silent rounding.\n\nExponents are accepted and folded into the scale (dec\"1.5e-3\" is Decimal{4,4,Int32}), and trailing zeros are significant: dec\"1.50\" has scale 2 while dec\"1.5\" has scale 1 (they compare equal). A literal that cannot be represented exactly — more than 77 significant digits, or a magnitude past the widest tier — is an ArgumentError at parse time, not a rounded value.\n\nBecause the type is minimal-fitting, a literal used in arithmetic promotes to whatever the other operand needs; annotate explicitly (DataDecimals.Decimal64{2}(\"1.25\"), or convert) when a column type is wanted.\n\n\n\n\n\n","title":"DataDecimals.@dec_str"},{"category":"function","location":"api/#DataDecimals.rescale","page":"API Reference","text":"DataDecimals.rescale(D::Type{<:Decimal}, x, mode=RoundNearest) -> D\nDataDecimals.rescale(x::DataDecimals.DecimalValue, s::Integer, mode=RoundNearest) -> DataDecimals.DecimalValue\n\nMove a decimal to another scale, rounding with mode when the target scale cannot hold x exactly. The first form targets a Decimal type D (whose scale is a type parameter); the second returns a DataDecimals.DecimalValue at the runtime scale s, which must be in 0:16383.\n\nmode is any of RoundNearest (half-even, the default), RoundNearestTiesAway, RoundNearestTiesUp, RoundToZero, RoundFromZero, RoundDown, RoundUp, or DataDecimals.RoundExact, which throws InexactError instead of dropping a digit. Rounding up in scale is always exact, so mode only matters when digits are dropped; overflowing the target's precision still throws OverflowError.\n\nconvert/constructors are the exact-or-throw counterpart, and round(D, x, mode) is the same operation spelled Base's way.\n\njulia> DataDecimals.rescale(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2356\"), RoundUp)\n1.24\n\njulia> DataDecimals.rescale(DataDecimals.DecimalValue(12345, 3), 1)\n12.3\n\n\n\n\n\n","title":"DataDecimals.rescale"},{"category":"function","location":"api/#DataDecimals.divide","page":"API Reference","text":"DataDecimals.divide(x, y, mode=RoundNearest)\n\nDivide two decimals, rounding the true quotient at scale max(scale(x), scale(y)) with mode. x / y is exactly DataDecimals.divide(x, y, RoundNearest) (half-even) — there is no global rounding state, so a different mode is always an argument, never a context.\n\nmode is any of RoundNearest, RoundNearestTiesAway, RoundNearestTiesUp, RoundToZero, RoundFromZero, RoundDown, RoundUp. Dividing by zero throws DivideError; a quotient too large for the result type throws OverflowError. For Decimal operands the result type is derived from the operand parameters (the widest quotient that can occur, capped at the 76-digit tier); for DataDecimals.DecimalValue operands it keeps the promoted storage type and the runtime scale max(scale(x), scale(y)).\n\njulia> DataDecimals.divide(DataDecimals.Decimal64{2}(\"1.00\"), DataDecimals.Decimal64{2}(\"3.00\"))\n0.33\n\njulia> DataDecimals.divide(DataDecimals.Decimal64{2}(\"1.00\"), DataDecimals.Decimal64{2}(\"3.00\"), RoundUp)\n0.34\n\n\n\n\n\n","title":"DataDecimals.divide"},{"category":"function","location":"api/#DataDecimals.normalize","page":"API Reference","text":"normalize(x) -> DataDecimals.DecimalValue\n\nThe value of x with all trailing zeros removed from the coefficient and the scale reduced to match (scales never go below zero, so integer values keep scale 0): normalize(dec\"1.2000\") == DataDecimals.DecimalValue(12, 1).\n\nThe result is always a DataDecimals.DecimalValue — the scale is a property of the value, not of the type — and keeps the input's storage type. The numeric value is unchanged (normalize(x) == x always), so this only matters where the representation is observable: the digits string prints, or a wire format that transmits the scale.\n\n\n\n\n\n","title":"DataDecimals.normalize"},{"category":"constant","location":"api/#DataDecimals.RoundExact","page":"API Reference","text":"DataDecimals.RoundExact\n\nA rounding mode that requires the operation to be exact. Wherever a mode is taken, RoundExact never rounds: DataDecimals.rescale, round(D, x, RoundExact), round(I, x, RoundExact), round(x, RoundExact; digits), DataDecimals.divide, and div, rem, and divrem with a mode throw InexactError when a digit would be dropped. In the Parsers extension, Parsers.parse throws InexactError for a value the target scale cannot hold exactly, Parsers.tryparse returns nothing, and Parsers.parsenext returns Parsers.RC_INVALID. Scaling up is always exact, so trailing zeros never fail.\n\njulia> DataDecimals.rescale(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2300\"), DataDecimals.RoundExact)\n1.23\n\njulia> DataDecimals.rescale(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2345\"), DataDecimals.RoundExact)\nERROR: InexactError: convert(Decimal64{2}, Decimal{18,4,Int64}(\"1.2345\"))\n[...]\n\n\n\n\n\n","title":"DataDecimals.RoundExact"},{"category":"function","location":"api/#DataDecimals.unscaled","page":"API Reference","text":"DataDecimals.unscaled(x) -> Integer\n\nThe unscaled coefficient of a decimal: x == unscaled(x) * 10^-scale(x). The returned integer is the value's storage type, and is exactly the coefficient an Arrow/Parquet/DuckDB buffer stores. reinterpret(typeof(x), unscaled(x)) rebuilds x.\n\n\n\n\n\n","title":"DataDecimals.unscaled"},{"category":"function","location":"api/#DataDecimals.scale","page":"API Reference","text":"DataDecimals.scale(x) -> Int\nDataDecimals.scale(::Type{<:Decimal}) -> Int\n\nThe scale (count of fractional decimal digits) of a decimal value: the S parameter for a Decimal, the stored per-value scale for a DataDecimals.DecimalValue. The companion precision is Base.precision, which is defined for Decimal types only. scale accepts a Decimal type as well as a value, since the scale is a type parameter there.\n\n\n\n\n\n","title":"DataDecimals.scale"},{"category":"function","location":"api/#DataDecimals.decimallength","page":"API Reference","text":"DataDecimals.decimallength(x) -> Int\n\nNumber of bytes writedecimal! produces for x (sign, digits, and decimal point included) — equivalently ncodeunits(string(x)). Computed from the coefficient's digit count and the scale, without formatting anything, so it is the size to reserve before writing.\n\n\n\n\n\n","title":"DataDecimals.decimallength"},{"category":"function","location":"api/#DataDecimals.writedecimal!","page":"API Reference","text":"DataDecimals.writedecimal!(buf, pos, x) -> Int\n\nWrite the plain decimal form of x into the byte buffer buf starting at pos; returns the first position after the written bytes. The caller must ensure decimallength(x) bytes of room — nothing is bounds-checked or grown.\n\nThe output is always the positional form (-1234.56, 0.0005), never scientific notation, so it is directly usable as a MySQL parameter, a CSV field, or a JSON number. This is the allocation-free counterpart to string(x), which is implemented on top of it.\n\njulia> buf = zeros(UInt8, 16);\n\njulia> n = DataDecimals.writedecimal!(buf, 1, DataDecimals.Decimal64{2}(\"-1.25\"))\n6\n\njulia> String(buf[1:n-1])\n\"-1.25\"\n\n\n\n\n\n","title":"DataDecimals.writedecimal!"},{"category":"section","location":"manual/#Manual","page":"Manual","text":"","title":"Manual"},{"category":"section","location":"manual/#Choosing-a-type","page":"Manual","text":"There are two value types, and the choice between them is about where the scale lives.\n\nDecimal{P,S,T} puts the scale in the type. It holds one field, unscaled::T, and means unscaled * 10^-S with the invariant |unscaled| < 10^P. P is the precision (total decimal digits), S the scale (fractional digits, 0 <= S <= P), and T the storage integer. A vector stores coefficients contiguously. Arrow buffers with the same width and host byte order can share this layout. Parquet byte arrays use big-endian two's-complement coefficients and require encoding. DuckDB uses 16/32/64/128-bit storage for precisions up to 4/9/18/38; it has no 256-bit decimal tier. See the Arrow format, Parquet decimal specification, and DuckDB numeric types.\n\nFour aliases name this package's storage tiers at their maximum precision:\n\nalias expands to digits storage typical use\nDataDecimals.Decimal32{S} Decimal{9,S,Int32} 9 32-bit narrow Arrow/Parquet columns\nDataDecimals.Decimal64{S} Decimal{18,S,Int64} 18 64-bit money, most SQL DECIMAL\nDataDecimals.Decimal128{S} Decimal{38,S,Int128} 38 128-bit DuckDB/Spark default width\nDataDecimals.Decimal256{S} Decimal{76,S,DataDecimals.Int256} 76 256-bit Arrow DataDecimals.Decimal256\n\nWrite Decimal{P,S} when a schema pins a precision narrower than its tier's maximum: Decimal{7,2} accepts fewer digits than DataDecimals.Decimal32{2} before throwing, though both store an Int32. The storage type is filled in from P whenever you leave it off, so Decimal{9,2} is another spelling of DataDecimals.Decimal32{2}.\n\nDataDecimals.DecimalValue{T} is the runtime-scale sibling: it stores the scale (an Int32 in 0:16383) alongside the coefficient. Use it when the scale travels with each value rather than with the column, as with a PostgreSQL numeric's per-value dscale, a value whose scale is not known until run time, or the result of DataDecimals.normalize. It is still isbits, but it is a word wider and its arithmetic has to align scales at run time, so Decimal{P,S} is the better default for columnar data.\n\nBoth are subtypes of DataDecimals.AbstractDecimal <: Real. There is no AbstractFloat subtyping, and there are no NaN or Inf values: isnan is always false, isfinite always true, and an operation that would produce a non-value throws.","title":"Choosing a type"},{"category":"section","location":"manual/#Constructing-values","page":"Manual","text":"dec\"1.25\"                          # literal, minimal fitting type\ndec\"1_000_000.25\"                  # underscores allowed between digit groups\ndec\"1.5e-3\"                        # exponents fold into the scale\n\nDataDecimals.Decimal64{2}(\"1234.56\")            # from a string (exact core scanner)\nusing Parsers                      # parse/tryparse come from the Parsers extension\nparse(DataDecimals.Decimal64{2}, \"1234.567\")    # 1234.57, rounds half-even at the target scale\ntryparse(DataDecimals.Decimal64{2}, \"nope\")     # nothing\n\nDataDecimals.Decimal64{2}(5)                    # from an Integer: exact or throws\nDataDecimals.Decimal64{2}(1//4)                 # from a Rational: exact or throws\nDataDecimals.Decimal64{2}(0.1)                  # from a Float: rounds half-even at scale 2\n\nreinterpret(DataDecimals.Decimal64{2}, 123456)  # from an unscaled coefficient, unchecked\n\nThe rule is exact-or-throw, with two documented exceptions. Construction from an AbstractFloat rounds half-even at the target scale, as cross-representation float conversions in Base do; a DataDecimals.DecimalValue built from a float instead keeps the exact binary expansion, which is what a runtime scale is for:\n\nDataDecimals.Decimal64{2}(0.1)   # 0.10\nDataDecimals.DecimalValue(0.1)   # 0.1000000000000000055511151231257827021181583404541015625\n\nString construction and parse also round half-even at the target scale. convert(D, x) follows the numeric constructor: float sources round, while integer, rational, and decimal sources are exact or throw. Use an exact string literal or rational source when binary float rounding is not intended.\n\n@dec_str produces the minimal type that fits the literal, so dec\"1.25\" is a Decimal{3,2,Int32} and not a money type. That suits a constant in an expression, where it promotes to whatever the other operand needs; annotate the type explicitly for a column.\n\nreinterpret is the zero-copy path: it builds a value directly from an unscaled coefficient without checking the |u| < 10^P invariant, because in a wire decode the invariant is the producer's contract and re-checking it per element is the cost being avoided.","title":"Constructing values"},{"category":"section","location":"manual/#Arithmetic","page":"Manual","text":"+ and - promote both operands value-preservingly and then compute checked: a result that no longer fits throws OverflowError. They never wrap and never silently round.\n\nDataDecimals.Decimal64{2}(\"1.20\") + DataDecimals.Decimal64{2}(\"0.05\")    # 1.25  :: Decimal{18,2,Int64}\ntypemax(DataDecimals.Decimal64{2}) + DataDecimals.Decimal64{2}(\"1.00\")   # OverflowError\n\n* is exact: the result has scale S1 + S2 and precision min(P1 + P2, 76). Multiplying by a plain Integer is scale-preserving instead, because multiplying by a count should not widen the scale.\n\nDataDecimals.Decimal64{2}(\"1.25\") * DataDecimals.Decimal64{3}(\"2.500\")   # 3.12500 :: Decimal{36,5,Int128}\nDataDecimals.Decimal64{2}(\"2.50\") * 3                       # 7.50    :: Decimal{37,2,Int128}\n\n/ rounds half-even at scale max(S1, S2). DataDecimals.divide is the same operation with the rounding mode spelled out:\n\nDataDecimals.Decimal64{2}(\"1.00\") / DataDecimals.Decimal64{2}(\"3.00\")                     # 0.33\nDataDecimals.divide(DataDecimals.Decimal64{2}(\"1.00\"), DataDecimals.Decimal64{2}(\"3.00\"), RoundUp)     # 0.34\n\nsum widens to the Int128 tier, where overflow is provably impossible below 1.7e20 elements, so the accumulation loop needs no per-element check and vectorizes while staying safe:\n\nsum(fill(DataDecimals.Decimal64{2}(\"0.10\"), 10))    # 1.00 :: Decimal{38,2,Int128}\n\ndiv, rem, mod, fld, cld, and divrem are real implementations rather than float fallbacks, and take a rounding mode where Base's do. Elementwise a .+ b, a .- b, and a .* b over same-shaped vectors hit SIMD kernels that compute per lane and check overflow once for the whole array.","title":"Arithmetic"},{"category":"section","location":"manual/#Rounding","page":"Manual","text":"There is no global rounding state in the package. Every rounding decision is an argument at a call site, and the seven modes accepted are RoundNearest (half-even, the default), RoundNearestTiesAway, RoundNearestTiesUp, RoundToZero, RoundFromZero, RoundDown, and RoundUp. An eighth, DataDecimals.RoundExact, requires exactness: it throws InexactError instead of dropping a digit.\n\n# change the scale of a value\nDataDecimals.rescale(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2356\"), RoundUp)   # 1.24\nDataDecimals.rescale(DataDecimals.DecimalValue(12345, 3), 1)                       # 12.3\nround(DataDecimals.Decimal64{2}, 1.005)                               # 1.00 (same thing, Base spelling)\n\n# round within a type\nround(DataDecimals.Decimal64{4}(\"1.2346\"); digits=2)                  # 1.2300\ntrunc(DataDecimals.Decimal64{2}(\"1.99\"))                              # 1.00\nfloor(DataDecimals.Decimal64{2}(\"-1.01\"))                             # -2.00\nceil(DataDecimals.Decimal64{2}(\"1.01\"))                               # 2.00\n\n# round out to an integer\nround(Int, DataDecimals.Decimal64{2}(\"1.50\"))                         # 2\nfloor(Int, DataDecimals.Decimal64{2}(\"1.99\"))                         # 1\n\nround(x; digits) stays exact and in-type; Base's generic fallback for a custom Real returns a Float64, and that is overridden here. There is no sigdigits keyword, since a fixed-point type has no floating significand.\n\nconvert between decimal types is the exact-or-throw counterpart of DataDecimals.rescale; reach for DataDecimals.rescale when the intent is \"round this into that scale\".","title":"Rounding"},{"category":"section","location":"manual/#Comparison-and-hashing","page":"Manual","text":"Equality and ordering are by numeric value, across scales, precisions, storage types, and other Real types. The representation still remembers its scale, so string prints the trailing zeros, but the number does not:\n\ndec\"1.20\" == dec\"1.2\" == 12//10              # true\nhash(DataDecimals.Decimal64{2}(\"1.25\")) == hash(1.25)     # true\nsort([dec\"3.5\", dec\"1.25\", dec\"2.0\"])        # works; sorts on the coefficient\n\nHashing goes through Base.decompose, so a decimal hashes identically to an equal Int, Rational, or Float64, which is what makes Dict keys, unique, and DataFrames groupby/join behave.\n\nBetween decimals, ≈ is exact equality, because Base's rtoldefault is zero for a non-AbstractFloat Real. That is the same behaviour Rational has, and it is arguably what money wants; pass rtol or atol for a tolerance. Comparing against a Float64 brings that type's default tolerance with it, again as for Rational.","title":"Comparison and hashing"},{"category":"section","location":"manual/#Printing-and-parsing","page":"Manual","text":"string and print always produce the plain positional form, which is a valid SQL literal, CSV field, and JSON number, and never scientific notation:\n\nstring(DataDecimals.Decimal64{2}(\"-12.30\"))    # \"-12.30\"\n\nshow, and therefore repr, gives the round-trippable typed form, Decimal{18,2,Int64}(\"-12.30\"). At the REPL, though, values display through the text/plain method, which prints plain digits and only switches to scientific notation once the positional form passes 44 characters:\n\njulia> DataDecimals.Decimal64{2}(\"1234.56\")\n1234.56\n\njulia> DataDecimals.DecimalValue{DataDecimals.Int256}(123, 50)\n1.23E-48\n\nparse and tryparse, defined by the Parsers extension, accept an optional sign, digits, an optional decimal point, and an optional e/E exponent, with surrounding ASCII whitespace allowed. They round half-even into the target scale. The string constructors (DataDecimals.Decimal64{2}(\"1.25\")) and dec\"...\" literals use the package's own exact scanner and need no extension.","title":"Printing and parsing"},{"category":"section","location":"manual/#The-wire-API","page":"Manual","text":"Four public names cover byte-level interop:\n\nusing DataDecimals: unscaled, scale, writedecimal!, decimallength\n\nx = DataDecimals.Decimal64{2}(\"-1.25\")\nunscaled(x)                       # -125 :: Int64, what the buffer stores\nscale(x)                          # 2\nreinterpret(typeof(x), unscaled(x)) === x   # true\n\ndecimallength(x)                  # 5, bytes needed, computed without formatting\nbuf = zeros(UInt8, 16)\npos = writedecimal!(buf, 1, x)    # 6, the position after the written bytes\nString(buf[1:pos-1])              # \"-1.25\"\n\nwritedecimal! is the allocation-free formatter that string is built on; it bounds-checks nothing, so reserve decimallength(x) bytes first. Together with reinterpret, this is enough to read and write decimal columns without materialising an intermediate String.\n\nDataDecimals.normalize is the other representation-level tool: it strips trailing zeros from the coefficient and lowers the scale to match, returning a DataDecimals.DecimalValue, since the scale is then a property of the value rather than the type. The number is unchanged; only the digits a wire format would carry are.\n\nnote: Note\nnormalize is public but not exported, so that it does not collide with LinearAlgebra.normalize; call it as DataDecimals.normalize.","title":"The wire API"},{"category":"section","location":"manual/#Extensions","page":"Manual","text":"These load automatically as package extensions when the companion package is present.","title":"Extensions"},{"category":"section","location":"manual/#Parsers.jl","page":"Manual","text":"Byte-level parse, tryparse, and parsenext over strings or byte spans, with decimal= and rounding= keywords. This is the entry point for a CSV reader or wire decoder.\n\nusing DataDecimals, Parsers\n\nParsers.parse(DataDecimals.Decimal64{2}, \"1234.56\")                    # 1234.56\nParsers.parse(DataDecimals.Decimal64{2}, \"1,25\"; decimal=',')          # 1.25\nParsers.parse(DataDecimals.Decimal64{2}, \"1.005\"; rounding=RoundUp)    # 1.01\nParsers.tryparse(DataDecimals.Decimal64{2}, \"nope\")                    # nothing\nParsers.tryparse(DataDecimals.Decimal64{2}, \"1.005\"; rounding=DataDecimals.RoundExact)  # nothing\n\nbuf = codeunits(\"12.34,56.78\")\nParsers.parsenext(DataDecimals.Decimal64{2}, buf, 1, length(buf))      # (12.34, 6, Parsers.RC_OK)\n\nparsenext returns (value, nextpos, returncode) and is the tokenizer entry point: it stops at the first byte that cannot continue a decimal, so the caller can step through a delimited line. It requires Parsers 3. Under Parsers 2 the extension loads as a no-op, so that an environment which also contains a Parsers-2-pinned package still resolves, and parse/tryparse are unavailable until Parsers 3 is (string constructors and literals still work). The extension also defines Base.parse/Base.tryparse for decimal types, so plain parse(DataDecimals.Decimal64{2}, s) is the same code path once Parsers is loaded.","title":"Parsers.jl"},{"category":"section","location":"manual/#Printf.jl","page":"Manual","text":"%f, %e, and %g round the decimal value half-even at the requested precision before conversion to BigFloat. Binary precision grows with the requested output precision. This preserves decimal ties and long output. %d is exact-or-InexactError, matching Rational.\n\n@printf(\"%.2f\", DataDecimals.Decimal64{2}(\"1234.56\"))   # 1234.56\n@printf(\"%d\", DataDecimals.Decimal64{2}(\"1234.00\"))     # 1234\n@printf(\"%d\", DataDecimals.Decimal64{2}(\"1234.56\"))     # InexactError","title":"Printf.jl"},{"category":"section","location":"manual/#JSON.jl","page":"Manual","text":"DataDecimals serialize as JSON numbers carrying their exact digits, neither as strings nor Float64-rounded (the default path for an unknown Real stringifies convert(Float64, x)). Requires JSON 1.x.\n\nJSON.json((price = DataDecimals.Decimal64{2}(\"1234.56\"), qty = 3))\n# {\"price\":1234.56,\"qty\":3}","title":"JSON.jl"},{"category":"section","location":"manual/#LinearAlgebra.jl","page":"Manual","text":"A*B, dot, tr, A+B, and kron stay exact decimal, widening the result type as the product requires. Factorizations, namely lu, cholesky, qr, svd, eigen, hessenberg, schur, lq, and the functions built on them (det, logdet, inv, eigvals, svdvals, cond, nullspace, pinv, rank), convert to Float64 first. See LinearAlgebra under Semantics for why. If you need an exact factorization, lu(Rational.(A)) gives one.","title":"LinearAlgebra.jl"},{"category":"section","location":"manual/#Integration-boundaries","page":"Manual","text":"Only Decimal, Decimal64, and @dec_str are exported. Other public names use the DataDecimals. namespace. Explicit imports such as using DataDecimals: DecimalValue are supported. sign(x) returns the storage integer's sign, so it works even when the decimal type cannot hold 1. Mixing a runtime-scale DecimalValue with a rational promotes to Rational{BigInt} so deep scales stay exact.\n\nThe fixed family supports 1 <= P <= 76 and 0 <= S <= P. Arrow schemas can carry negative scales or scales above their precision; those schemas need an explicit adapter policy and cannot be mapped directly to Decimal{P,S}. DecimalValue permits scales through 16383, but its coefficient is still bounded by its storage integer. It does not cover all PostgreSQL numeric values, including NaN, infinities, or coefficients beyond 256 bits.\n\nValidate schema parameters, coefficients, byte order, and null masks before using reinterpret for external data. Null values belong in missing or a format's validity bitmap. A scalar reinterpret(D, u) is unchecked and can truncate a wider integer. Use convert(D, x) to convert a numeric value.\n\nThe LinearAlgebra extension supports ordinary dense matrices, pivot arguments, and diagonal/triangular inverses and solves with vector or dense matrix right sides. Other structured or in-place operations can still use fixed-scale arithmetic; convert inputs with float first. Use Rational{BigInt} inputs when exact algebra is required.","title":"Integration boundaries"},{"category":"section","location":"migration/#Migration-from-Decimals.jl","page":"Migration from Decimals.jl","text":"DataDecimals 1.0 is a separate fixed-point package. Decimals.jl 0.x remains a separate arbitrary-precision floating-point package. To migrate, replace using Decimals with using DataDecimals and review the arithmetic below.","title":"Migration from Decimals.jl"},{"category":"section","location":"migration/#What-changed,-and-why","page":"Migration from Decimals.jl","text":"The 0.x Decimal was a heap-backed arbitrary-precision floating decimal: Decimal <: AbstractFloat, holding a sign, a BigInt coefficient, and an exponent, with Python-style context semantics. Every operation rounded to a dynamically scoped precision, 28 significant digits by default, within exponent limits of ±999999.\n\nThat design has two problems for the applications that reach for a decimal type. The first is the silent rounding: a 31-digit sum quietly loses three digits and nothing says so, which is the opposite of the guarantee a ledger or a wire format needs. The second is the representation: a heap-allocated BigInt coefficient cannot be a column type, so decimal data arriving from Arrow, Parquet, DuckDB, or a SQL driver had to be converted element by element on the way in and on the way out.\n\nVersion 1.0 addresses both. Decimal{P,S,T} is isbits with the precision and scale in the type, matching the (precision, scale, width) triple those systems already use, so a Vector of them is a column buffer; and arithmetic is exact and checked, with rounding available only as an explicit argument. DataDecimals.DecimalValue{T} covers the row-oriented case where the scale travels per value.\n\nThe full API comparison, including three bugs found in 0.5.1 while writing it, is in docs/comparison-with-0.5.1.md in the repository.","title":"What changed, and why"},{"category":"section","location":"migration/#What-keeps-working-unchanged","page":"Migration from Decimals.jl","text":"The name Decimal.\n==, <, <=, cmp, and hash by numeric value: dec\"1.20\" == dec\"1.2\" still holds, and hashes still agree with equal Ints, Rationals, and Float64s.\nparse / string round-trips for ordinary values, with parse now provided by the Parsers extension.\nround(x; digits).\nzero, one, abs, unary -, signbit, iszero, isfinite, isnan.\ndec\"...\" literals, including _ digit separators.\nDataDecimals.normalize for stripping trailing zeros.\nScientific-notation display for extreme exponents (past 44 characters of positional form).","title":"What keeps working unchanged"},{"category":"section","location":"migration/#What-needs-a-change","page":"Migration from Decimals.jl","text":"0.x 1.0\nDecimal(1.25), one untyped decimal choose a type: DataDecimals.Decimal64{2}(\"1.25\") for a fixed-scale column, DataDecimals.DecimalValue when the scale is per value, dec\"1.25\" for a literal\nsilent rounding to context precision on +/-/* exact and checked; opt into rounding with DataDecimals.rescale(D, x, mode) or DataDecimals.divide(x, y, mode)\nContext, with_context, @with_context, CONTEXT, rounding(Decimal) removed; rounding is an argument at each call site\nDivisionByZeroError, UndefinedDivisionError Base's DivideError\nDecimal <: AbstractFloat Decimal <: Real; the AbstractFloat claim is what produced 0.x's floatmin/floatmax/Inf bug reports\nprecision(x) = the context's digit budget precision(x) = the type's P; DataDecimals.scale(x) for the scale\nround(x; sigdigits) not applicable; a fixed-point type has no floating significand, so use digits\nnumber (exported but undefined in 0.5.1) gone; use Float64(x), Rational(x), or Int(x)\nblanket T(x) = T(BigFloat(x)) conversions exact-or-throw per family, with correctly rounded float conversions; 0.5.1's blanket route returned 10^80 for BigInt(Decimal(10^80 + 1))","title":"What needs a change"},{"category":"section","location":"migration/#Porting-a-context-block","page":"Migration from Decimals.jl","text":"Where 0.x code set a context to control rounding:\n\n# 0.x\nwith_context(Context(precision=10, rounding=RoundUp)) do\n    a / b\nend\n\nthe 1.0 form names the target scale and the mode at the call site:\n\n# 1.0\nDataDecimals.divide(DataDecimals.rescale(DataDecimals.Decimal64{10}, a),\n                DataDecimals.rescale(DataDecimals.Decimal64{10}, b), RoundUp)\n\nIt is more to write, and in exchange the result no longer depends on which caller it runs inside.","title":"Porting a context block"},{"category":"section","location":"migration/#Porting-arithmetic-that-relied-on-silent-rounding","page":"Migration from Decimals.jl","text":"If 0.x code accumulated values and let the context trim them, the 1.0 equivalent is to say where the trimming happens:\n\n# 0.x: the context rounded this back to 28 digits\ntotal = a * b + c\n\n# 1.0: exact by default, so either keep the exact wide result…\ntotal = a * b + c                        # scale S_a+S_b, checked\n\n# …or say what scale you want\ntotal = DataDecimals.rescale(DataDecimals.Decimal64{2}, a * b + c) # half-even into 2 decimal places","title":"Porting arithmetic that relied on silent rounding"},{"category":"section","location":"migration/#What-is-lost","page":"Migration from Decimals.jl","text":"Values beyond 76 significant digits or scale 16383, and dynamically scoped precision. If you need unbounded exactness, Rational{BigInt}(x), which is what big(x) returns for a decimal, is the escape hatch, and it is the same one Base offers for Rational arithmetic that outgrows its integer type.","title":"What is lost"},{"category":"section","location":"migration/#What-is-new","page":"Migration from Decimals.jl","text":"Worth knowing about coming from 0.x:\n\nThe DataDecimals.Decimal32 / DataDecimals.Decimal64 / DataDecimals.Decimal128 / DataDecimals.Decimal256 tiers, with precision and scale carried in the type.\nDataDecimals.DecimalValue for runtime scales.\nThe zero-copy wire API: DataDecimals.unscaled, DataDecimals.scale, reinterpret construction, writedecimal!, and decimallength. Arrow buffers can share the same layout; other wire formats require their own encoding.\nThe explicit rounding API: DataDecimals.rescale, DataDecimals.divide, and round(D, x, mode), in seven modes and with no global state.\nReal div / rem / mod / fld / cld / divrem implementations in every rounding mode; 0.x had only AbstractFloat fallbacks.\nExact Rational conversions in both directions, correctly rounded Float64 / Float32 / Float16, and exact BigFloat at any requested precision and mode.\nThe Parsers, Printf, JSON, and LinearAlgebra extensions.\nSIMD broadcast kernels for +, -, * and a vectorized sum.\ntypemin / typemax / eps / floatmin / floatmax / widen.","title":"What is new"},{"category":"section","location":"migration/#A-note-for-downstream-packages","page":"Migration from Decimals.jl","text":"LibPQ.jl pins DataDecimals = 0.4.1, a pre-0.5 API that already stopped working with 0.5.x, so 1.0 is no worse for it, and DataDecimals.DecimalValue is the natural target for a PostgreSQL numeric, whose scale travels per value.","title":"A note for downstream packages"},{"category":"section","location":"#DataDecimals.jl","page":"Home","text":"Exact fixed-point decimal numbers for Julia.\n\nDecimal{P,S,T} is an isbits value holding one integer coefficient, with the precision P and scale S fixed in the type. A vector stores these coefficients contiguously. Arrow buffers with the same width and host byte order can share that layout. Other wire formats require format-specific encoding; see the wire API in the manual.\n\nArithmetic is exact and checked. +, -, and * neither round nor wrap; they throw OverflowError when a result does not fit. Where rounding is unavoidable it takes a RoundingMode argument at the call site, rather than reading a global context.","title":"DataDecimals.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"DataDecimals\")\n\nJulia 1.10 or later. There are no runtime dependencies outside the standard library; the 256-bit storage integers (DataDecimals.Int256/UInt256) are implemented in the package, on LLVM's native wide-integer arithmetic.","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using DataDecimals\n\n# Construct: from a literal, from a string, or from a wire coefficient\ndec\"1.25\"                              # Decimal{3,2,Int32}, the minimal fitting type\nDataDecimals.Decimal64{2}(\"1234.56\")                # Decimal{18,2,Int64}, a money column\nreinterpret(DataDecimals.Decimal64{2}, 123456)      # 1234.56, zero-copy from an Arrow buffer\n\n# + and - keep the scale and are checked; * is exact; / rounds\nDataDecimals.Decimal64{2}(\"1.20\") + DataDecimals.Decimal64{2}(\"0.05\")     # 1.25    :: Decimal{18,2,Int64}\nDataDecimals.Decimal64{2}(\"1.25\") * DataDecimals.Decimal64{3}(\"2.500\")    # 3.12500 :: Decimal{36,5,Int128}\nDataDecimals.Decimal64{2}(\"1.00\") / DataDecimals.Decimal64{2}(\"3.00\")     # 0.33    (half-even at scale 2)\nsum(fill(DataDecimals.Decimal64{2}(\"0.10\"), 10))             # 1.00    :: Decimal{38,2,Int128}\n\n# Rounding is an argument, not a mode to switch on\nDataDecimals.divide(DataDecimals.Decimal64{2}(\"1.00\"), DataDecimals.Decimal64{2}(\"3.00\"), RoundUp)   # 0.34\nDataDecimals.rescale(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2356\"), RoundUp)        # 1.24\nround(DataDecimals.Decimal64{4}(\"1.2346\"); digits=2)                       # 1.2300\n\n# DataDecimals.DecimalValue carries its scale per value, like a PostgreSQL numeric dscale\nDataDecimals.DecimalValue(12345, 3)                 # 12.345\nDataDecimals.rescale(DataDecimals.DecimalValue(12345, 3), 1)     # 12.3\nDataDecimals.normalize(dec\"1.2000\")        # 1.2 :: DataDecimals.DecimalValue{Int32}\n\n# Printing and conversion\nstring(DataDecimals.Decimal64{2}(\"-12.30\"))         # \"-12.30\", always positional\nFloat64(DataDecimals.Decimal64{2}(\"1.25\"))          # 1.25, correctly rounded\nRational(DataDecimals.Decimal64{2}(\"1.25\"))         # 5//4, exact\n\nParsing lives in the Parsers extension, so parse and tryparse need using Parsers alongside using DataDecimals:\n\nusing DataDecimals, Parsers\n\nparse(DataDecimals.Decimal64{2}, \"1234.567\")        # 1234.57, half-even at the target scale\ntryparse(DataDecimals.Decimal64{2}, \"nope\")         # nothing\n\nString constructors and dec\"...\" literals use the package's own exact scanner and work without any extension.\n\nOverflow, inexactness, and division by zero are errors:\n\ntypemax(DataDecimals.Decimal64{2}) + DataDecimals.Decimal64{2}(\"1.00\")   # OverflowError\nconvert(DataDecimals.Decimal64{2}, DataDecimals.Decimal64{4}(\"1.2345\"))  # InexactError (use DataDecimals.rescale)\nDataDecimals.Decimal64{2}(\"1.00\") / zero(DataDecimals.Decimal64{2})      # DivideError","title":"Quick start"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Manual covers choosing a type, constructing values, arithmetic, rounding, printing, the wire API, and the package extensions.\nSemantics states the rules for every operation, and what the ecosystem study found when these types were taken through DataFrames, Statistics, LinearAlgebra, and their neighbours.\nMigration from Decimals.jl lists what changed from the 0.x Decimals package, with replacements for the old API.\nAPI Reference lists every exported and public name.","title":"Where to go next"},{"category":"section","location":"#Further-reading-in-the-repository","page":"Home","text":"bench/PERF.md: benchmark method and results, with charts in bench/charts/.\ndocs/ecosystem-compat.md: behaviour across the Julia package ecosystem.\ndocs/comparison-with-0.5.1.md: API comparison with Decimals 0.5.1.\n\nLicensed under the MIT License.","title":"Further reading in the repository"}]
}
